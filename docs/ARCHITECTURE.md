# 🏗️ TAS EVENT VOTE SYSTEM - ARCHITECTURE DIAGRAM

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER DEVICES                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Desktop    │  │    Tablet    │  │  Smartphone  │          │
│  │   Browser    │  │   Browser    │  │   Browser    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────┬──────────────────────────────┬──────────────────────────┘
          │                              │
          │   HTTP/HTTPS                 │
          │                              │
┌─────────▼──────────────────────────────▼──────────────────────────┐
│                   NODE.JS SERVER (Port 3000)                      │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Express.js Server                                         │  │
│  │  - Static File Serving (HTML, CSS, JS)                   │  │
│  │  - CORS Middleware                                        │  │
│  │  - API Proxy to PHP Backend                              │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─────────────────────────┐    ┌──────────────────────────┐    │
│  │   Served Pages          │    │   External Libraries    │    │
│  ├─────────────────────────┤    ├──────────────────────────┤    │
│  │ • index.html (Home)     │    │ • TailwindCSS (CDN)     │    │
│  │ • admin.html (Admin)    │    │ • Chart.js (CDN)        │    │
│  │ • vote.html (Voting)    │    │ • QR Server API         │    │
│  │ • results.html (Results)│    │                          │    │
│  └─────────────────────────┘    └──────────────────────────┘    │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │   JavaScript Modules                                     │    │
│  ├──────────────────────────────────────────────────────────┤    │
│  │ • admin.js      - Admin dashboard logic                 │    │
│  │ • vote.js       - Voting page logic                     │    │
│  │ • results.js    - Results page logic                    │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                    HTTP/REST API Calls
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                   PHP SERVER (Port 8000)                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  RESTful API Endpoints                                    │ │
│  │                                                            │ │
│  │  Event Management                                         │ │
│  │  ├─ GET    /api/event                                    │ │
│  │  ├─ PUT    /api/event                                    │ │
│  │  └─ PUT    /api/event-status                             │ │
│  │                                                            │ │
│  │  Candidate Management                                     │ │
│  │  ├─ GET    /api/candidates                               │ │
│  │  ├─ POST   /api/candidates                               │ │
│  │  ├─ PUT    /api/candidates/{id}                          │ │
│  │  └─ DELETE /api/candidates/{id}                          │ │
│  │                                                            │ │
│  │  Participant Management                                   │ │
│  │  ├─ GET    /api/participants                             │ │
│  │  └─ POST   /api/participants                             │ │
│  │                                                            │ │
│  │  Voting System                                            │ │
│  │  ├─ GET    /api/votes                                    │ │
│  │  └─ POST   /api/votes                                    │ │
│  │                                                            │ │
│  │  Results                                                  │ │
│  │  └─ GET    /api/results                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Business Logic                                           │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ • Event configuration                                     │ │
│  │ • Candidate management                                    │ │
│  │ • Participant registration & validation                  │ │
│  │ • Vote processing & duplication prevention               │ │
│  │ • Point calculation & aggregation                         │ │
│  │ • Results computation                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                         File I/O Operations
                               │
┌──────────────────────────────▼──────────────────────────────────┐
│                   JSON FILE STORAGE                             │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  /data/ Directory                                         │ │
│  │                                                            │ │
│  │  event.json                                               │ │
│  │  ├─ Event name & description                            │ │
│  │  ├─ Max participants limit                              │ │
│  │  └─ Current status (setup/running/closed)               │ │
│  │                                                            │ │
│  │  candidates.json                                          │ │
│  │  ├─ Candidate ID, name, description                     │ │
│  │  └─ Display order                                        │ │
│  │                                                            │ │
│  │  participants.json                                        │ │
│  │  ├─ Participant ID, name, phone, email                  │ │
│  │  ├─ Authentication token                                │ │
│  │  └─ Vote status & timestamp                             │ │
│  │                                                            │ │
│  │  votes.json                                               │ │
│  │  ├─ Vote ID, participant ID, candidate ID               │ │
│  │  ├─ Vote level (1/2/3)                                  │ │
│  │  ├─ Points awarded (15/10/5)                            │ │
│  │  └─ Vote timestamp                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────┐
│   VOTER      │
└──────┬───────┘
       │ 1. Scans QR Code
       │    or visits /vote
       ▼
┌──────────────────────┐
│  Registration Page   │
│  (vote.html)         │
└──────┬───────────────┘
       │ 2. Enters name & phone
       │    POST /api/participants
       ▼
┌──────────────────────┐
│  Voting Page         │
│  (vote.html)         │
│  Shows candidates    │
└──────┬───────────────┘
       │ 3. Selects voting level
       │    for each candidate
       ▼
┌──────────────────────┐
│  Vote Submission     │
│  POST /api/votes     │
│  (repeats for        │
│   each candidate)    │
└──────┬───────────────┘
       │ 4. Votes stored
       │    in votes.json
       ▼
┌──────────────────────┐
│  Success Page        │
│  (vote.html)         │
└──────┬───────────────┘
       │ 5. View results
       │    GET /api/results
       ▼
┌──────────────────────┐
│  Results Page        │
│  (results.html)      │
│  Shows:              │
│  • Podium (top 3)    │
│  • Rankings          │
│  • Statistics        │
│  • Charts            │
└──────────────────────┘
```

---

## Admin Flow Diagram

```
┌──────────────┐
│    ADMIN     │
└──────┬───────┘
       │ 1. Access admin.html
       ▼
┌────────────────────────┐
│  Admin Dashboard       │
│  (admin.html)          │
│  4 Tabs:               │
└──────┬─────────────────┘
       │
       ├─→ 1. Event Settings
       │   ├─ Configure event
       │   ├─ Set max participants
       │   └─ Generate QR code
       │
       ├─→ 2. Manage Candidates
       │   ├─ Add candidates
       │   ├─ Edit details
       │   └─ Delete candidates
       │
       ├─→ 3. Control & Monitor
       │   ├─ View statistics
       │   ├─ List participants
       │   └─ Track votes
       │
       └─→ 4. Live Results
           ├─ Podium display
           ├─ Rankings table
           └─ Charts & stats
           
Each action triggers API calls:
├─ PUT    /api/event
├─ PUT    /api/event-status
├─ POST   /api/candidates
├─ PUT    /api/candidates/{id}
├─ DELETE /api/candidates/{id}
├─ GET    /api/participants
└─ GET    /api/results
```

---

## Real-Time Update Flow

```
┌────────────────┐
│  Voter Submits │
│  Votes         │
└────────┬───────┘
         │
         │ POST /api/votes
         │
         ▼
┌────────────────┐
│  PHP Backend   │
│  - Validate    │
│  - Calculate   │
│  - Save to     │
│    votes.json  │
└────────┬───────┘
         │
         │ Response: Vote saved
         │
         ▼
┌────────────────────────┐
│  Results Page          │
│  (Any Open Browser)    │
│  - Auto-refresh every  │
│    2-3 seconds         │
│  - GET /api/results    │
└────────┬───────────────┘
         │
         │ Fetch aggregated results
         │
         ▼
┌────────────────────────┐
│  Display Updated:      │
│  ├─ Podium             │
│  ├─ Rankings           │
│  ├─ Statistics         │
│  └─ Charts             │
│                        │
│  ~2 second latency     │
└────────────────────────┘
```

---

## File Structure Hierarchy

```
c:\Project\TAS-Event-Vote\
│
├── 📚 Documentation
│   ├── START_HERE.md
│   ├── QUICK_START.md
│   ├── README.md
│   ├── PROJECT_SUMMARY.md
│   ├── COMPLETION_CHECKLIST.md
│   ├── TEST_SCENARIOS.md
│   ├── FILE_LISTING.md
│   ├── INDEX.md
│   └── COMPLETION_REPORT.md
│
├── 🚀 Server Setup
│   ├── server.js
│   ├── package.json
│   ├── setup.bat
│   ├── setup.sh
│   └── launcher.bat
│
├── 🌐 Frontend Layer (public/)
│   ├── index.html          └─→ Home Page
│   ├── admin.html          └─→ Admin Dashboard
│   ├── vote.html           └─→ Voting Interface
│   ├── results.html        └─→ Results Display
│   │
│   ├── js/
│   │   ├── admin.js        └─→ Admin Logic
│   │   ├── vote.js         └─→ Vote Logic
│   │   └── results.js      └─→ Results Logic
│   │
│   └── lib/                └─→ External Libraries
│
├── 🔌 API Layer (api/)
│   ├── index.php           └─→ API Endpoints
│   ├── config.php          └─→ Configuration
│   └── .htaccess           └─→ Routing Rules
│
├── 💾 Data Layer (data/)
│   ├── event.json
│   ├── candidates.json
│   ├── participants.json
│   └── votes.json
│
├── 🔧 Configuration
│   └── .gitignore

LEGEND:
├─ File
├─→ Represents
└─ Folder
```

---

## Technology Stack Diagram

```
┌─────────────────────────────────────────────────────┐
│           PRESENTATION LAYER                        │
│  ┌──────────────────────────────────────────────┐  │
│  │  TailwindCSS   Chart.js    Vanilla JS        │  │
│  │  (Styling)     (Charts)    (Interactivity)  │  │
│  └──────────────────────────────────────────────┘  │
└────────────────┬──────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────┐
│           APPLICATION LAYER                       │
│  ┌──────────────────────────────────────────────┐ │
│  │  HTML5          Node.js/Express              │ │
│  │  (Markup)       (Server)                     │ │
│  └──────────────────────────────────────────────┘ │
└────────────────┬──────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────┐
│           BUSINESS LOGIC LAYER                    │
│  ┌──────────────────────────────────────────────┐ │
│  │  PHP 7.4+                                   │ │
│  │  • Validation                               │ │
│  │  • Calculation                              │ │
│  │  • Data Processing                          │ │
│  └──────────────────────────────────────────────┘ │
└────────────────┬──────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────┐
│           DATA LAYER                              │
│  ┌──────────────────────────────────────────────┐ │
│  │  JSON Files                                 │ │
│  │  (No Database Required)                     │ │
│  └──────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## Component Interaction Matrix

```
                  │ Admin │ Voter │ Results │ API │
─────────────────┼───────┼───────┼─────────┼─────┤
Event Config     │  ✅   │  ❌   │   ✅    │  ✅ │
Candidates       │  ✅   │  ✅   │   ✅    │  ✅ │
Participants     │  ✅   │  ✅   │   ✅    │  ✅ │
Votes            │  ✅   │  ✅   │   ✅    │  ✅ │
Results          │  ✅   │  ✅   │   ✅    │  ✅ │
QR Code          │  ✅   │  ❌   │   ❌    │  ❌ │
Live Updates     │  ✅   │  ✅   │   ✅    │  ✅ │

✅ = Accesses/Uses
❌ = Does not access
```

---

## Security Architecture

```
┌─ Request ─────────────────────────────────────┐
│                                                │
│  ┌─ CORS Validation                         │
│  │  ├─ Origin check                         │
│  │  └─ Method validation                    │
│  ▼                                           │
│  ┌─ Input Validation                        │
│  │  ├─ Required fields                      │
│  │  ├─ Data type check                      │
│  │  └─ Length validation                    │
│  ▼                                           │
│  ┌─ Token Verification                      │
│  │  ├─ Token existence                      │
│  │  └─ Token validity                       │
│  ▼                                           │
│  ┌─ Business Logic                          │
│  │  ├─ Duplicate checks                     │
│  │  ├─ Limit enforcement                    │
│  │  └─ Data integrity                       │
│  ▼                                           │
└─ Response ────────────────────────────────────┘
   with validation
```

---

**System Architecture - December 3, 2024**
**TAS Event Vote System v1.0**
